// Header is no longer used on the timer tab.
// Navigation is handled by the bottom tab bar in app/(tabs)/_layout.tsx.
// The top bar (app name + add session button) is rendered directly in app/(tabs)/index.tsx.
export default function Header() {
    return null;
}
